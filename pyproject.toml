[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "btc5-v3-ingestion"
version = "0.4.0"
description = "Isolated market research, admissibility and post-hoc analytics; no trading"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

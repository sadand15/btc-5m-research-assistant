[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "btc5-v3-ingestion"
version = "0.3.0"
description = "Isolated ingestion, edge math and candidate admissibility; no trading"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
